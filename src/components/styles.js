import {
    StyleSheet,
} from 'react-native'

const styles = StyleSheet.create({
    container: {
        // width: '45%',
        padding: 7,
        margin: 3,
        marginBottom: 15,
        borderWidth: 1,
        borderColor: '#333'
    },
    wrapper: {
        flexDirection: 'row'
    },
    title: {
        marginRight: 10,
        fontSize: 15,
    },
    content: {

    }
})

export default styles